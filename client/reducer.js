const initialState = {}

const reducer = (state = initialState, action) => {
  // const newState = {...state}

  switch (action.type){
    // case GOT_STUDENTS:
    //   return newState
  default:
    return state
  }
}

export default reducer
